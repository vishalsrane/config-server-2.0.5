---
server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vishalsrane/config-server
          search-paths:
          - 'station*'